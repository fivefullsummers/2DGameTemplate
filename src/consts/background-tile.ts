/**
 * Tile size for background.png (1024x1024). Used by CheapBackground and
 * MobileControlsBar so the pattern aligns when the bar sits below the game canvas.
 */
export const BACKGROUND_TILE_SIZE = 1024;
